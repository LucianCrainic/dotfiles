
## Useful notes when stuff goes wrong
### FZF not showing when using ctrl-r
    rm -rfv ~/.fzf
    git clone --depth 1 https://github.com/junegunn/fzf.git ~/.fzf
    ~/.fzf/install
    cd ~/.fzf && git pull && ./install
    PERFECTLY WORKING! GREAT!
